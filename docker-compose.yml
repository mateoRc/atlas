services:
  atlas:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./content:/content:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:8080/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
